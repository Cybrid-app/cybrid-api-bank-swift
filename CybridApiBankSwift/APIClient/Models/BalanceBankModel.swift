//
// BalanceBankModel.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct BalanceBankModel: Codable, JSONEncodable, Hashable {

    /** The total posted debits on the account, in base units. */
    public var debits: Int
    /** The total posted credits on the account, in base units. */
    public var credits: Int
    /** The balance on the account, in base units. */
    public var amount: Int

    public init(debits: Int, credits: Int, amount: Int) {
        self.debits = debits
        self.credits = credits
        self.amount = amount
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case debits
        case credits
        case amount
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(debits, forKey: .debits)
        try container.encode(credits, forKey: .credits)
        try container.encode(amount, forKey: .amount)
    }
}

