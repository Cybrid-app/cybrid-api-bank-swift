//
// TransactionBankModel.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct TransactionBankModel: Codable, JSONEncodable, Hashable {

    public enum DirectionBankModel: String, Codable, CaseIterable, CaseIterableDefaultsLast {
        case credit = "credit"
        case debit = "debit"
        case unknownDefaultOpenApi = "unknown_default_open_api"
    }
    /** The unique identifier for the platform account. */
    public var accountGuid: String
    /** The transaction amount in base units. */
    public var amount: Int
    /** The asset code, e.g., USD. */
    public var asset: String
    /** The unique identifier for the bank. */
    public var bankGuid: String
    /** The unique identifier for the customer. Absent for bank-owned accounts. */
    public var customerGuid: String?
    /** The direction of the transaction; one of credit or debit. */
    public var direction: DirectionBankModel
    /** The type of ledger line item, e.g., principal or fee. */
    public var category: String?
    public var resource: TransactionResourceBankModel?
    /** Whether the transaction has posted. */
    public var posted: Bool
    public var balance: BalanceBankModel?
    /** ISO8601 datetime the record was created at. */
    public var createdAt: Date

    public init(accountGuid: String, amount: Int, asset: String, bankGuid: String, customerGuid: String? = nil, direction: DirectionBankModel, category: String? = nil, resource: TransactionResourceBankModel? = nil, posted: Bool, balance: BalanceBankModel? = nil, createdAt: Date) {
        self.accountGuid = accountGuid
        self.amount = amount
        self.asset = asset
        self.bankGuid = bankGuid
        self.customerGuid = customerGuid
        self.direction = direction
        self.category = category
        self.resource = resource
        self.posted = posted
        self.balance = balance
        self.createdAt = createdAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accountGuid = "account_guid"
        case amount
        case asset
        case bankGuid = "bank_guid"
        case customerGuid = "customer_guid"
        case direction
        case category
        case resource
        case posted
        case balance
        case createdAt = "created_at"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(accountGuid, forKey: .accountGuid)
        try container.encode(amount, forKey: .amount)
        try container.encode(asset, forKey: .asset)
        try container.encode(bankGuid, forKey: .bankGuid)
        try container.encodeIfPresent(customerGuid, forKey: .customerGuid)
        try container.encode(direction, forKey: .direction)
        try container.encodeIfPresent(category, forKey: .category)
        try container.encodeIfPresent(resource, forKey: .resource)
        try container.encode(posted, forKey: .posted)
        try container.encodeIfPresent(balance, forKey: .balance)
        try container.encode(createdAt, forKey: .createdAt)
    }
}

