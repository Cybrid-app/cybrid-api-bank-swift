//
// FeeDetailBankModel.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct FeeDetailBankModel: Codable, JSONEncodable, Hashable {

    public enum TypeBankModel: String, Codable, CaseIterable, CaseIterableDefaultsLast {
        case bank = "bank"
        case platform = "platform"
        case network = "network"
        case unknownDefaultOpenApi = "unknown_default_open_api"
    }
    /** The type of fee; one of bank, platform, or network. */
    public var type: TypeBankModel
    /** The asset the fee is denominated in, e.g., USD. */
    public var asset: String
    /** The fee amount in base units of the asset. */
    public var amount: Int

    public init(type: TypeBankModel, asset: String, amount: Int) {
        self.type = type
        self.asset = asset
        self.amount = amount
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case type
        case asset
        case amount
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(type, forKey: .type)
        try container.encode(asset, forKey: .asset)
        try container.encode(amount, forKey: .amount)
    }
}

